node_modules/
dist/
.env
models/*.bin
models/*.gguf
*.log
.DS_Store
.vite/
.pids/
logs/
.playwright-mcp/
docker-compose.test.yml
frontend/nginx.workshop.conf

# Plan #6 ingest target — sources dropped here at runtime
raw/*
!raw/.gitkeep
state/current.json
.superpowers/

# Staging area for USB distribution artifacts (multi-GB Docker image tarballs)
usb-staging/

# Host-specific Compose overlays:
#   - .override.yml: standard Compose dev-time customizations (bind mounts, etc.)
#   - .blackwell-tts.yml: auto-generated by install.sh when sm_120 GPU detected
docker-compose.override.yml
docker-compose.blackwell-tts.yml

# Plan #7 — runtime-saved custom personality (saved from the UI)
state/custom-personality.json
